{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.2.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "test": "lerna run test --stream",
    "publish": "lerna publish"
  }
}
